# Rust build outputs
/target/
**/*.rs.bk

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp
*.swo

# Personal scratch — not for tracked content
docs/dev-notes/
docs/plans/

# Cairn runtime artifacts (V2 daemon will write here once Phase 1 ships)
.cairn/

# Code Briefcase V1 (Python) runtime artifacts — V1 still operates on this repo for dogfooding
.code-briefcase/
.code-briefcaseignore

# macOS metadata
._*
.Spotlight-V100
.Trashes

# Environment / secrets
.env
.env.local
.env.*.local
