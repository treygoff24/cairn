//! `cairn-config` — deterministic config loading, config hash, and feature flags.
//!
//! Bootstrap stub. Real loading, protocol-version pin, strict/default/advisory modes,
//! and a config hash stable across key ordering land in Wave 1.1 Task 3.
