//! `cairn-vcs` — `RepoEpoch`: git operation-state capture.
//!
//! Bootstrap stub. Real detection (normal, detached HEAD, merge, rebase, cherry-pick,
//! bisect, non-git) and head-OID / branch-ref / index-tree capture land in
//! Wave 1.1 Task 6. Must not panic on partial states or outside a git repo.
